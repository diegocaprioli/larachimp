checks:
    php: true

build:
    tests:
        override:
            -
                command: 'vendor/bin/phpunit tests --coverage-clover=coverage-file'
                coverage:
                    file: 'coverage-file'
                    format: 'clover'
